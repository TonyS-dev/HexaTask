pluginManagement {
    repositories {
        mavenCentral()
        maven { url = 'https://repo.spring.io/snapshot' }
        gradlePluginPortal()
    }
}

rootProject.name = 'hexatask'
include 'backend'